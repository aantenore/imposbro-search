{{- if .Values.adminUi.autoscaling.enabled -}}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "imposbro-search.fullname" . }}-admin-ui
  labels:
    {{- include "imposbro-search.labels" . | nindent 4 }}
    app.kubernetes.io/component: admin-ui
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "imposbro-search.fullname" . }}-admin-ui
  minReplicas: {{ .Values.adminUi.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.adminUi.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.adminUi.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.adminUi.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.adminUi.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.adminUi.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
  {{- with .Values.adminUi.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
