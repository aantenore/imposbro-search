# IMPOSBRO SEARCH - Environment Variables

# --- Kafka Configuration ---
KAFKA_BROKER_URL=kafka:29092
KAFKA_TOPIC_PREFIX=imposbro_search_sharded

# --- Typesense Configuration (for the default cluster) ---
TYPESENSE_HOST=typesense
TYPESENSE_PORT=8108
TYPESENSE_API_KEY=xyz

# --- Redis & Caching Configuration ---
REDIS_URL=redis://redis:6379
DEFAULT_QUERY_CACHE_TTL_SECONDS=60

# --- Admin UI & Service Discovery Configuration ---
# Used by the Next.js server for server-side proxying (container-to-container communication)
INTERNAL_QUERY_API_URL=http://query_api:8000